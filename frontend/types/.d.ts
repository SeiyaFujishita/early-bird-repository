// types/tw-elements.d.ts
declare module "tw-elements" {
  // 'tw-elements' の具体的な型定義をここに書く
  // 例:
  export function someFunction(arg: string): number;
  // 上記は 'tw-elements' が someFunction という関数をエクスポートし、
  // その関数が string 型の引数を取り、number を返すということを表しています。
}
